import { Experience, Formation } from '../../interface/Interface.Profil';

interface AddArrayElementProps {
    returnTabs: () => Experience[];
}

const AddArrayElemnt = ({ returnTabs }: AddArrayElementProps) => {
    return <></>;
};

export default AddArrayElemnt;
